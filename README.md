

# МамаРаспознай — анализатор детского плача с AI

Telegram-бот, который помогает родителям понять: *почему плачет ребёнок?*

Использует **бесплатную ML модель от Hugging Face** для анализа детского плача с помощью искусственного интеллекта.

## 🤖 Возможности

- ✅ Принимает голосовые сообщения через Telegram
- ✅ Анализирует плач с помощью **бесплатной ML модели** (Hugging Face)
- ✅ Определяет вероятные причины: голод, усталость, боль/дискомфорт
- ✅ Полностью приватно — аудио не сохраняется
- ✅ Работает локально (модель загружается один раз)

## 🛠 Как запустить

### 1. Установите Python 3.9+

### 2. Установите `ffmpeg`:

- **Windows**: https://www.gyan.dev/ffmpeg/builds/ → распакуйте и добавьте `bin/` в PATH
- **Mac**: `brew install ffmpeg`
- **Ubuntu**: `sudo apt install ffmpeg`

### 3. Создайте виртуальное окружение (рекомендуется):

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows
```

### 4. Установите зависимости:

```bash
pip install -r requirements.txt
```

**Примечание**: При первом запуске бот автоматически загрузит ML модель (~1.5 GB). Это займёт несколько минут, но произойдёт только один раз.

### 5. Создайте Telegram бота:

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot` и следуйте инструкциям
3. Скопируйте полученный токен

### 6. Настройте переменные окружения:

Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=ваш_токен_от_BotFather
HF_TOKEN=  # Опционально: токен Hugging Face (не обязателен для базового использования)
```

### 7. Запустите бота:

```bash
python bot.py
```

Вы увидите сообщение: `🚀 CryCheck бот запущен! Откройте Telegram и отправьте /start`

## 📱 Использование

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Запишите голосовое сообщение с плачем ребёнка (10-30 секунд)
4. Получите анализ с рекомендациями

## 🔬 Как это работает

Бот использует:
- **ML модель**: `MIT/ast-finetuned-audioset-10-10-0.4593` от Hugging Face (бесплатная)
- **Аудио-признаки**: спектральный анализ, MFCC, zero crossing rate, темп
- **Комбинированный анализ**: результаты ML модели + аудио-признаки для точного определения причины

### Определяемые причины:

- 🍼 **Голод** — ритмичный, умеренный плач
- 😴 **Усталость** — мягкий, затухающий плач
- ⚠️ **Боль/Дискомфорт** — резкий, пронзительный крик

## 🔒 Приватность

- Все аудио файлы обрабатываются локально
- Временные файлы удаляются сразу после анализа
- Никакие данные не отправляются на внешние серверы (кроме загрузки модели при первом запуске)
- ML модель работает полностью локально после загрузки

## 🛠 Технические детали

- **Библиотеки**: aiogram, transformers, torch, librosa, pydub
- **ML модель**: Audio Spectrogram Transformer (AST) от MIT
- **Формат аудио**: 16kHz, моно, WAV
- **Fallback**: Если ML модель недоступна, используется простой анализ на основе аудио-признаков

## ⚠️ Важные замечания

- Это **инструмент для помощи**, а не замена медицинской консультации
- При серьёзных симптомах всегда обращайтесь к врачу
- Первая загрузка модели может занять время (зависит от скорости интернета)
- Для работы требуется ~2 GB свободного места на диске (для модели)

## 📝 Лицензия

Проект использует бесплатные библиотеки и модели с открытым исходным кодом.

